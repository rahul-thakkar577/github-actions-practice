name: Setup and Greet
description: Greet user and show system info

inputs:
  name:
    description: "User name"
    required: true
  language:
    description: "Language"
    default: en

outputs:
  greeted:
    description: "Greeting status"
    value: "true"

runs:
  using: "composite"
  steps:
    - name: Greeting
      run: |
        if [ "${{ inputs.language }}" = "en" ]; then
          echo "Hello ${{ inputs.name }}"
        else
          echo "Hi ${{ inputs.name }}"
        fi
      shell: bash

    - name: System Info
      run: |
        echo "Date: $(date)"
        echo "Runner OS: $RUNNER_OS"
      shell: bash